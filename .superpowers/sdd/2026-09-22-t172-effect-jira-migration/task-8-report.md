# Task 8 — pure publication plan verified

The paused checkpoint already contained the Task 8 implementation. No archive was reapplied and no storage or database integration was added. Preparation decodes the manifest, accepted configuration, source refs and environment; binds the manifest checksum and accepted configuration revision; validates mapping and collision decisions; and produces deterministic entity and attachment identities. Finalization requires settled attachment outcomes, freezes native documents and exact index rows, and emits canonical plan bytes with the external SHA-256 publication revision. The archive/report contract includes every manifest-v2 category and applies the exclusion decision before permanent output.

The focused pure-domain suite passed: `bun run test -- src/Jira/Adf.test.ts src/Jira/Manifest.test.ts src/Jira/Mappings.test.ts src/Jira/Preflight.test.ts src/Jira/PublicationPlan.test.ts src/Jira/Report.test.ts src/Jira/Import.test.ts` from `packages/backend` (93 tests; run together with the five fixture tests). Backend `bun run typecheck` exited 0. `vp fmt` on Task 8 files and focused `vp lint` exited 0; lint reported nonfatal immutable-map and test assertion warnings. `git diff --check` passed. The final comprehensive review remains required by the user.

Task 9 must resolve referenced artifacts with checksum verification inside Activities, persist the prepared and final plan bytes, copy or explicitly skip attachments, and write exactly the frozen documents and rows. Task 10 owns transactional publication. Task 11 removes legacy compatibility. The cleanup write protocol and Effect retry wakeup remain separate open decisions.
