# SDD ledger — plan: docs/superpowers/plans/2026-09-22-t172-effect-jira-migration.md

## Identity and execution status

- Worktree: `/Users/woutmissler/Web/project-project-t172-effect`.
- Original PR base / whole-rewrite review base: `994455a26be958db47826c7425efedfdf84aea90`.
- Planning restoration commit: `4d06422f`.
- Preflight helper: `/root/preflight`, model `gpt-6-astra`, reasoning `high`, explicitly dispatched with `fork_turns=none`; **not a task implementer**. No implementation, commits, subagents, main-checkout changes, or preview access performed.
- Controller: `/root`; ratification and all task dispatch/review bookkeeping remain controller-owned.
- P01–P25 below were reviewed and ratified by the controller before Task 1. Explicit architecture approval boundaries remain unchosen and binding.
- Read: full plan, design spec, AGENTS.md, docs/PROJECTPROJECT.md, PRODUCT.md, DESIGN.md, SDD skill and task-reviewer rubric. Targeted local source checks inspected workflow finalizer/memory-engine contracts, configuration schema, existing worker/credential lease usage, manifest, and button primitive.
- Spec is reachable and authoritative for the migration. General project document contains historical Effect v3/BaseUI advice; current AGENTS and task design explicitly choose existing Effect v4 stack. No dependencies or shared HTTP changes are authorized by this ledger.

## Baseline evidence (controller-reported)

- `bun install`: complete, no tracked changes.
- Root and serial backend typecheck: exit 1 from six pre-existing TS377033 `multipleEffectProvide` warnings in `Client.test.ts` at 180, 234, 291, 371, 435, 485. Remediation belongs to Task 5, which already edits that file; earlier task reports must distinguish this baseline from regressions.
- Serial frontend typecheck: exit 0. Initial frontend exit 137 was cancellation after root backend failure, not a reproduced frontend type error.
- Full baseline tests with `DATABASE_URL` and `PROJECTPROJECT_TEST_DATABASE_URL` unset: 172 files passed, 12 skipped; 1543 tests passed, 65 skipped. Existing experimental SQLite and jsdom canvas/scroll warnings are baseline observations, not new defects. Database-gated tests remain unproved until disposable SQL runs.

## Task checklist

- [x] Task 1: Persisted Workflow Contracts and In-Memory Runtime Proof
- [x] Task 2: Hidden-Project Schema and Visibility Barrier
- [x] Task 3: Fenced Migration Projection and Execute-First Handshake
- [x] Task 4: Deterministic Artifact Store and Manifest v2
- [x] Task 5: Single-Page Jira Client with Typed Rate Limits
- [x] Task 6: Durable Scan Activities and Configuration Pause
- [ ] Task 7: Server-Side Drafts, Run, Retry, Reconnect, Rescan, and Cancel
- [x] Task 8: Complete Preflight and Immutable Publication Plan
- [ ] Task 9: Idempotent Hidden Materialization Activities
- [ ] Task 10: Atomic Publication and Cancellation Linearization
- [ ] Task 11: Cleanup Workflow, Retention, and Production Runtime Cutover
- [ ] Task 12: Wizard Draft Persistence and Public Atom Polling
- [ ] Task 13: Disposable Browser Harness
- [ ] Task 14: End-to-End Release Gate and Branch Cleanup

Tasks 1–6 are complete and reviewed, as detailed below. Task 8 passed focused verification after the pause; Task 13 fixtures passed but its harness is pending. Task 7 remains blocked on the Effect retry wakeup. Tasks 9–14 remain pending. Historical entries below retain their original context.

## Exact persisted contracts to carry into briefs

| Contract | Design authority | Plan comparison |
| --- | --- | --- |
| Migration tag | `ProjectProject/JiraMigration/v1` | T1 matches. |
| Cleanup tag | `ProjectProject/JiraMigrationCleanup/v1` | T11 matches. |
| Start gate | `StartImport/v1/{scanRevision}` | T1/T6/T7 match; global statement that all deferred prefixes use `v1/...` conflicts. P01. |
| Retry gate | `Retry/v1/{failureSequence}` | T1/T6/T7 match; same global-prefix conflict. P01. |
| Start/finalize Activities | `v1/start`, `v1/finalize` | T1 body directly invokes callbacks; callbacks must really construct durable Activities. P02. |
| Project/field scan Activities | `v1/scan/project`, `v1/scan/fields` | No conflict; spec uses distinct executions for rescans. |
| Issue-page Activity | `v1/scan/issues/{scanRevision}/{pageOrdinal}/{cursorHash}/{operationTry}` | T6 inserts extra `root`; T11 sample is additionally missing hash/try. P01. |
| Comment-page Activity | `v1/scan/comments/{scanRevision}/{issueId}/{pageOrdinal}/{cursorHash}/{operationTry}` | T6 generic parent form can match comments; encode source ID safely. P01. |
| Manifest/preflight | `v1/build-manifest/{scanRevision}`, `v1/import/preflight/{scanRevision}` | Preserve exact design forms, never raw manifest in workflow history. |
| Attachment Activity | `v1/import/attachment/{jiraAttachmentId}/{operationTry}` | T9 lacks a complete naming example; carry this design form. |
| Documents/archive/publish | `v1/import/write-documents/{publicationRevision}`, `v1/import/write-archive/{publicationRevision}`, `v1/import/publish/{publicationRevision}` | T9 bounded document batches need deterministic subordinate identities; define publicationRevision before use. P10. |
| Create key | derived from organization/user/request | T1 fixes `create:{organizationId}:{userId}:{requestId}`; source conflict must still be checked. |
| Rescan key | `{migrationId}:{workflowAttempt}` | T1 matches; success must return existing migrationId, not new executionId. P02. |
| Cleanup key | migration/mode/generation | T11 fixes `{migrationId}:{mode}:{cleanupGeneration}`, generation = accepted CAS revision. P08. |
| Scan key fixture | deterministic organization storage path | T4 fixes `migrations/jira/m1/scan-3/raw/issues/page-0004-abc123.json`; operationTry changes Activity identity, not logical artifact identity. |
| Permanent archive | `imports/jira/{migrationId}/` | T9 matches; private staging refs must not become public URLs or leak excluded bodies. |
| Versions | immutable persisted schemas | Manifest v2, publication plan v1; preserve/version old decoders rather than silently reinterpret bytes. P11. |

## Task self-consistency scan

| Task | Produces / consumes within task | Files, tests, code and constraints checked | Finding / proposed disposition |
| --- | --- | --- | --- |
| 1 | Consumes source/errors; produces schemas, names, injectable workflow/Activities. | Tests demand real replay, finalization and captured defects; body calls callbacks, returns executionId, awaits fixed revision 1; schema declarations occur after Workflow.make. | P01/P02/P03: exact names, runnable dependency order, actual Activity execution, correct rescan identity; memory replay preserves the same engine history. Counter example alone is incomplete. |
| 2 | Consumes all public project access paths; produces nullable publishedAt, text migration ID, fencing columns and predicate. | Schema adds nullable column then backfills createdAt; normal writes explicit now; no lease removal yet. Visibility tests name src/main.test.ts absent from explicit file list. | P04: staged nullable migration before default; public-seam inventory includes routes/MCP and reconciler; add necessary named tests; retain lease compatibility until T11. |
| 3 | Consumes T1 workflow + T2 columns; produces public projection, CAS and handshake. | Shape snippet omits listed methods; sample returns owned row although public method returns detail; test compares beginRescan result workflowAttempt. Handler test absent from git add. | P05/P06: complete one projection interface and map owned rows to unchanged detail; include created handler test in commit; no public error widening. |
| 4 | Consumes OrgStorage/S3; produces refs, prefix deletion and manifest v2. | Checksum test has meaningful negative assertion; manifest upgrades affect old Scan/Import/Worker consumers before their later tasks. | P11/P12: versioned compatibility is needed for intermediate typechecks and old artifacts; isolate raw JSON in artifact data; bounded page deletion follows every token. |
| 5 | Consumes credentials/transport; produces one-page methods, rate-limit/transient errors. | Data.TaggedError sample is not a persisted Schema; repeated cursor detection needs request/current cursor context; max 86,400,000ms specified. | P03/P13: use schema-encoded Activity error boundary, distinguish one logical request from 401 refresh attempt; retain convenience API until consumers move; fix baseline warnings here. |
| 6 | Consumes fence/artifacts/client; produces durable page scan + manifest/config gate. | Result fixture excludes issueIds but stream sample reads page.issueIds; flatMap over IDs does not itself perform nested page calls; recursive catch wraps recursive retry. | P01/P07/P14: derive IDs from normalized chunk or explicit small contract; bound actual dependent work; monotonic generation per logical retry with no duplicate failure handling. |
| 7 | Consumes current generations; produces draft/run/retry/rescan/cancel commands and cleanup seam. | Incomplete arrays fit shared config, invalid destination fields do not; timeout example falls back to stale currentDetail; cleanup/cancel publication tests precede real T10/T11. | P05/P06/P08/P15: preserve schema, read latest detail on timeout; lightweight service seam tests now plus real SQL proof later; transition retry CAS before signal. |
| 8 | Consumes manifest v2/config; produces immutable plan, two-pass maps, deterministic IDs and archive/report. | Plan schema snippet omits exact index rows/source maps/documents mentioned in prose; deterministic ID sample only checks repeatability and one distinct input. | P10/P16: complete schema from prose before T9; strengthen behavior/golden/schema tests, privacy marker across all permanent outputs; no effects in pure preflight. |
| 9 | Consumes plan/ref/fence/publishedAt; produces bounded hidden materialization Activities. | Sample hidden insert is unfenced until later verify; plan requests batch writes but only one document Activity name; snapshot attachment decisions can change outcomes. | P10/P17: atomic fence check with row write, deterministic bounded batches; verify all outputs and private refs before publish; model partial outcomes before immutable final plan. |
| 10 | Consumes verified graph + tx; produces pure index rows + atomic publication. | Files omit MigrationProjection.ts despite finalizer work; signature shows caller tx yet implementation owns db.transaction; publication must replay commit-before-result safely. | P05/P18: allow focused projection change; one transaction ownership boundary; replay recognizes matching succeeded publication; row lock/CAS linearizes cancellation. |
| 11 | Consumes terminal states/artifacts/runtime; produces cleanup, retention, SQL cutover and deletes worker. | Cleanup generation claim/enqueue crash window unspecified; restart sample closes runtime gracefully; broad lease search also catches valid OAuth refresh leases; SQL layer example omits SqlClient provision. | P08/P19/P20/P21: recoverable cleanup handshake, actual abrupt-process proof, preserve credential leases, compose one shared runner/client, and preserve retryable cleanup scheduling. |
| 12 | Consumes unchanged API/whole draft; produces private atom refresh, DraftSaveRef and step variant. | Server revision can race step saves; shell button does not cover browser back/other route leave; combined transition utilities overwrite each other; bounded flush lacks bound. | P15/P22: serialize saves, use current form values and accepted revision, existing navigation mechanism for supported exits; combined transition properties; preserve theme/keyboard/i18n/atom conventions. |
| 13 | Consumes production API + fake transport/OAuth + disposable SQL/S3; produces fixtures/local harness. | Prefix-only validation does not prove local endpoints; injected OAuth authorization URL and backend signing/storage env need isolation; sample Left assumes Either-style validator. | P23: use actual Effect-v4 result shape, enforce loopback resources before side effects, inject through existing seam and keep controls absent from production. No automation dependency. |
| 14 | Consumes every preceding task; produces release evidence/cleanup. | Global wording says Task13 browser while task14 owns walkthrough; duplicate browser rerun without intervening changes; broad grep catches credential leases/historical SQL; tests may silently skip. | P19/P24/P25: actual SQL required, scope searches, exact PR-only assets, one post-final-code browser pass unless changes justify repeat; environment and evidence are release gates. |

## Pairwise dependency/interface scan

All 91 unordered pairs are enumerated so omissions are auditable. Shared files are derived from each task’s Files block; interface handoffs also cover files omitted by those blocks. A row marked no direct shared interface is included for completeness, not an invented dependency. Abbreviations: `J/` = `packages/backend/src/Jira/`, `L/` = `packages/backend/src/Layers/`, `B/` = `packages/backend/src/`, `F/` = `packages/frontend/src/`.

| Pair | Shared files / produces → consumes | Finding and proposed disposition |
| --- | --- | --- |
| 1–2 | workflow payload/execution ID → text ID and fence columns | Compatible; migrate schema before real projection writes. P04. |
| 1–3 | Shared: `J/MigrationWorkflow.ts`. workflow execute/names → execute-first public commands | P02/P05: initial migration ID is execution ID; rescan execution ID is distinct from migration ID. |
| 1–4 | small persisted Activity schemas ↔ artifact refs | P03/P12: share a single encoded reference shape; raw JSON never enters history. |
| 1–5 | Shared: `J/Blocked.ts`. persisted Activity failures ← client typed failures | P03: Data.TaggedError alone cannot define persisted error schema. |
| 1–6 | Shared: `J/MigrationActivities.ts`, `J/MigrationWorkflow.test.ts`, `J/MigrationWorkflow.ts`. workflow/Activity helpers → page scan and gates | P01/P02/P07/P14: correct names, real Activities, one retry generation per failed unit. |
| 1–7 | Shared: `J/MigrationWorkflow.ts`. StartImport/Retry tokens and interrupt → public command routing | P01/P02/P15: exact generations, rescan migration ID, real finalizer. |
| 1–8 | small versioned contracts ↔ publication-plan reference and failures | P10/P12: plan stays in storage; workflow carries ref and scalar results. |
| 1–9 | Shared: `J/MigrationActivities.ts`, `J/MigrationWorkflow.test.ts`, `J/MigrationWorkflow.ts`. Activity/name/fence contracts → materialization lifecycle | P01/P10/P17: version and freeze extra Activity identities before use. |
| 1–10 | Shared: `J/MigrationActivities.ts`, `J/MigrationWorkflow.ts`. finalizer/Activity lifecycle → publish/cancel result | P18: finalizer must not overwrite succeeded; replay final publication safely. |
| 1–11 | Shared: `J/Blocked.ts`, `J/MigrationWorkflow.ts`. workflow registration and schema contract → SQL runtime and cleanup definition | P02/P19/P21: production runner provides registered layers and same small contracts. |
| 1–12 | No direct shared file or interface; only transitive integration. | No additional pair-specific conflict. |
| 1–13 | workflow-backed production lifecycle → browser fixture API composition | Compatible through production JiraMigrations; harness cannot directly drive Workflow. |
| 1–14 | T1 completed behavior/tests → whole release gate | P19/P24/P25: inspect relevant evidence; real SQL cannot be silently skipped; release findings route to owning task. |
| 2–3 | text ID/fencing/retention columns → projection row codec/CAS | P04/P05: define legacy-row handling and fence defaults explicitly. |
| 2–4 | No direct shared file or interface; only transitive integration. | No additional pair-specific conflict. |
| 2–5 | No direct shared file or interface; only transitive integration. | No additional pair-specific conflict. |
| 2–6 | scanRevision/failureSequence columns → scan projection writes | Compatible; no destination creation while scanning. |
| 2–7 | revision/retention/cleanup fields → commands | P05/P08/P15: retry and cleanup share atomic ownership predicates. |
| 2–8 | project uniqueness/index schema → preflight and planned rows | P10/P16: collision checks include hidden reservations; preflight stays side-effect free. |
| 2–9 | publishedAt predicate + hidden-row identity → hidden materialization | P04/P17: explicit null and transactional ownership/fence validation. |
| 2–10 | Shared: `L/TicketIndex.ts`. project/index schema + public predicate → publication transaction/index builders | P18: same transaction makes canonical reads visible and migration succeeded. |
| 2–11 | Shared: `B/db/schema.ts`, `B/main.ts`. intermediate schema/main wiring → lease removal/runtime cutover | P04/P20/P21: remove only migration worker leases after restart proof; preserve visibility edits. |
| 2–12 | No direct shared file or interface; only transitive integration. | No additional pair-specific conflict. |
| 2–13 | public authorization/hidden predicates → real harness production routes | P23: fixture exercises actual production permissions and invisibility. |
| 2–14 | T2 completed behavior/tests → whole release gate | P19/P24/P25: inspect relevant evidence; real SQL cannot be silently skipped; release findings route to owning task. |
| 3–4 | projection artifact pointers/codecs ← manifest/ref contract | P05/P11/P12: versioned refs are private; public detail remains unchanged. |
| 3–5 | public projection failure reason ← Jira client errors | P03/P05: map private transport errors to existing public Jira failure taxonomy. |
| 3–6 | Shared: `J/MigrationProjection.ts`, `J/MigrationWorkflow.ts`. AttemptFence/advance/recordFailure → durable scan | P05/P07: all progress and failureSequence writes fenced; stale zero-row writes stop projection changes. |
| 3–7 | Shared: `J/MigrationProjection.ts`, `J/MigrationWorkflow.ts`, `J/Migrations.actions.test.ts`, `J/Migrations.test.ts`, `J/Migrations.ts`. owned/toDetail/actions/saveConfiguration/CAS → public commands | P05/P06/P15: one public detail mapping; no raw row return or stale timeout fallback. |
| 3–8 | projection saved config/manifest pointer → preflight; failure classification ← blockers | P06/P10/P16: frozen matching scan revision and complete validated plan. |
| 3–9 | Shared: `J/MigrationWorkflow.ts`. AttemptFence and destination metadata → hidden Activities | P17: validate fence and DB write atomically, not only after effect. |
| 3–10 | Shared: `J/MigrationWorkflow.ts`. terminal transition/CAS ← publication success/finalizer | P05/P18: expand finalizer interface and one authoritative transaction outcome. |
| 3–11 | Shared: `J/MigrationProjection.ts`, `J/MigrationWorkflow.ts`, `J/Migrations.ts`. claim/release/deleteAfterCleanup + owned state → cleanup/retention | P08/P21: generation-fenced claim, safe enqueue, delete projection last, retained recovery handle. |
| 3–12 | JiraMigrationDetail/actions/revision → atoms and wizard | P06/P15/P22: existing API shape unchanged; freshest revision for serialized draft saves. |
| 3–13 | public create/configure/run/cancel/discard → browser harness HTTP | P23: public seams only; no control endpoint bypass of projection ownership. |
| 3–14 | T3 completed behavior/tests → whole release gate | P19/P24/P25: inspect relevant evidence; real SQL cannot be silently skipped; release findings route to owning task. |
| 4–5 | artifact provenance page codec ← one-page client schemas | P11/P12/P13: decoded page records enter storage without response bodies in Activity results. |
| 4–6 | artifact refs/deterministic keys/manifest v2 → scan page writes/build manifest | P01/P07/P11/P12: retry changes Activity identity, not page artifact identity; dependent IDs read from normalized chunks. |
| 4–7 | manifest requirements/ref → configure/run/rescan | P06/P11: old saved configuration invalidated against new scanRevision. |
| 4–8 | manifest v2 and readJson checksum → pure publication plan/archive | P10/P11/P12/P16: complete every category and strip excluded content in permanent outputs. |
| 4–9 | artifact write/verify + deterministic object keys → materialization | P10/P12/P17: refs only in results; actual documents/archives remain stored. |
| 4–10 | plan checksum and verified object refs → publication guard | P18: identical checksum recognized on post-commit replay. |
| 4–11 | listPrefix/deletePrefix/private artifact graph → cleanup | P08/P21: delete exact mode-specific keys, preserve permanent successful archive and reset diagnostics. |
| 4–12 | No direct shared file or interface; only transitive integration. | No additional pair-specific conflict. |
| 4–13 | manifest categories/storage contract → fixtures and isolated bucket | P23: every category fixture, local isolated storage only. |
| 4–14 | T4 completed behavior/tests → whole release gate | P19/P24/P25: inspect relevant evidence; real SQL cannot be silently skipped; release findings route to owning task. |
| 5–6 | one-page methods + rate/transient errors → page Activities | P03/P07/P13/P14: one logical page per Activity; Schedule retries transient only; durable 429 sleep outside. |
| 5–7 | credential reconnect failure → reconnect_required/Retry command | P03/P15: resolve fresh initiating-user credentials on the next operationTry. |
| 5–8 | source schemas/attachment metadata → manifest normalization/preflight | Compatible via T4/T6 normalized records; no additional client calls from pure transformations. |
| 5–9 | fresh credentials/download request + typed errors → copyAttachment | P03/P10/P17: deterministic Jira-ID identity; no filename/size reuse. |
| 5–10 | No direct shared file or interface; only transitive integration. | No additional pair-specific conflict. |
| 5–11 | Shared: `J/Blocked.ts`. credentials + removed client retry loops → production workflow cutover | P20: credential refresh lease remains; only migration execution leases disappear. |
| 5–12 | public mapped reconnect/failure → wizard feedback | Compatible via unchanged API; do not expose private client errors directly. |
| 5–13 | transport/token service shapes → fake Jira/OAuth fixture | P13/P23: fixture uses actual decoder inputs and 401 refresh behavior. |
| 5–14 | T5 completed behavior/tests → whole release gate | P19/P24/P25: inspect relevant evidence; real SQL cannot be silently skipped; release findings route to owning task. |
| 6–7 | Shared: `J/MigrationProjection.ts`, `J/MigrationWorkflow.ts`. scan summary/current deferred generations → configure/run/retry/rescan | P01/P07/P15: signal generation and projection fence must agree. |
| 6–8 | manifest v2/requirements → preflight and plan | P07/P10/P11/P16: validate uniqueness/references before gate; archive category completeness end to end. |
| 6–9 | Shared: `J/MigrationActivities.ts`, `J/MigrationWorkflow.test.ts`, `J/MigrationWorkflow.ts`. workflow scan/gate → import Activities | P10/P14/P17: no hidden creation before accepted StartImport; unify bounded retry semantics. |
| 6–10 | Shared: `J/MigrationActivities.ts`, `J/MigrationWorkflow.ts`. workflow progression/finalizer → publication result | P18: projection may become succeeded only in final transaction, not scan or generic finalizer. |
| 6–11 | Shared: `J/MigrationProjection.ts`, `J/MigrationWorkflow.ts`. durable page/gate/sleep behavior → SQL restart tests/runtime | P01/P19/P21: exact complete names; real process death, not graceful scope close. |
| 6–12 | scanning/configuration public states → polling and draft UI | P22: poll public refresh command and render every AsyncResult branch. |
| 6–13 | page calls/rate limit/reconnect → deterministic browser scenarios | P07/P13/P23: fake server call counters by logical page; completed Activities replay once. |
| 6–14 | T6 completed behavior/tests → whole release gate | P19/P24/P25: inspect relevant evidence; real SQL cannot be silently skipped; release findings route to owning task. |
| 7–8 | whole saved config/current requirements → completeness/preflight | P06/P16: incomplete arrays persist; invalid field shapes remain existing validation errors. |
| 7–9 | Shared: `J/MigrationWorkflow.ts`. accepted run/rescan/cancel → materialization entry/fence | P08/P17/P18: reset cleanup blocks publication and removes only unpublished destination. |
| 7–10 | Shared: `J/MigrationWorkflow.ts`. interrupt/finalizeInterrupted → publication cancellation race | P18: command-level fakes do not replace real SQL race proof; succeeded is immutable. |
| 7–11 | Shared: `J/MigrationProjection.ts`, `J/MigrationWorkflow.ts`, `J/Migrations.ts`. JiraMigrationCleanupCommands/CleanupCommand → production cleanup implementation | P08/P21: explicit contract introduced before production implementation; no guessed stub behavior. |
| 7–12 | revisioned configure + bounded run response → complete-draft saves | P06/P15/P22: timeout observes latest detail; serialized saves update accepted revision. |
| 7–13 | public commands/reconnect/ownership → HTTP/browser scenarios | P23: controls only manipulate fake external system; migration changes through public API. |
| 7–14 | T7 completed behavior/tests → whole release gate | P19/P24/P25: inspect relevant evidence; real SQL cannot be silently skipped; release findings route to owning task. |
| 8–9 | Shared: `J/Import.test.ts`, `J/Import.ts`. immutable plan/ID maps/privacy decisions → materialization | P10/P16/P17: final attachment outcomes/ref rewrites resolved before final immutable docs/plan; preserve exclusion. |
| 8–10 | Shared: `J/Import.test.ts`, `J/Import.ts`. exact plan index rows/project/status/member/attachment identities → tx inserts | P10/P18: full plan schema must include rows and normalized docs; single pure row builder source. |
| 8–11 | archive decisions/permanent artifact identity → cleanup mode boundaries | P21: successful archive survives; private raw artifacts removed only after verification. |
| 8–12 | existing choices/preflight blockers → wizard mappings/restrictions | P06/P16/P22: preserve API and messages; any new public reason needs existing mapping or explicit approval. |
| 8–13 | complete archive/report categories → fixture coverage | P16/P23: include unique restricted marker and in-scope reference fixtures. |
| 8–14 | T8 completed behavior/tests → whole release gate | P19/P24/P25: inspect relevant evidence; real SQL cannot be silently skipped; release findings route to owning task. |
| 9–10 | Shared: `J/Import.test.ts`, `J/Import.ts`, `J/MigrationActivities.ts`, `J/MigrationWorkflow.ts`. verified hidden graph + pending attachments → atomic publish | P17/P18: real DB transaction validates current graph/fence/checksum; no outer Db service. |
| 9–11 | Shared: `J/MigrationWorkflow.ts`. hidden rows/object graph → reset/discard/expiry/post_success | P08/P17/P21: late bounded writes cannot recreate deleted graph; cleanup must coordinate interruption/fences. |
| 9–12 | public progress/waiting → UI feedback | Compatible through public detail; UI never reads materialization internals. |
| 9–13 | attachment materialization/cancellation → pause_attachment scenario | P17/P23: pause actual external response and prove public invisibility. |
| 9–14 | T9 completed behavior/tests → whole release gate | P19/P24/P25: inspect relevant evidence; real SQL cannot be silently skipped; release findings route to owning task. |
| 10–11 | Shared: `J/MigrationWorkflow.ts`. terminal publication/cancel result → finalizer and post-success cleanup | P18/P21: cleanup cannot downgrade success or remove published files; success survives commit-before-result. |
| 10–12 | succeeded destination/report → completion navigation | Compatible through unchanged detail; never navigate to hidden destination before success. |
| 10–13 | real publish transaction/read seams → browser completion | P18/P23: canonical reads, live attachments, archive/report all reachable after success. |
| 10–14 | T10 completed behavior/tests → whole release gate | P19/P24/P25: inspect relevant evidence; real SQL cannot be silently skipped; release findings route to owning task. |
| 11–12 | cleanup canDiscard/projection removal/retry ownership → UI actions/polling | P08/P21/P22: cleanup marker disables discard; failed cleanup restores handle; removed detail handled visibly. |
| 11–13 | production SqlClient/runner/workflow layers → standalone harness | P19/P21/P23: share production registration, replace external Jira dependencies only; isolated SQL/storage. |
| 11–14 | T11 completed behavior/tests → whole release gate | P19/P24/P25: inspect relevant evidence; real SQL cannot be silently skipped; release findings route to owning task. |
| 12–13 | public frontend/API contract → runnable browser backend | P22/P23: existing auth signup/signin and OAuth UI work with loopback harness; no Playwright adoption. |
| 12–14 | T12 completed behavior/tests → whole release gate | P19/P24/P25: inspect relevant evidence; real SQL cannot be silently skipped; release findings route to owning task. |
| 13–14 | T13 completed behavior/tests → whole release gate | P19/P24/P25: inspect relevant evidence; real SQL cannot be silently skipped; release findings route to owning task. |

Counts: 14 task-consistency rows; 91 pair rows; 85 pairs share a declared file or identified interface; 6 pairs explicitly checked without a direct handoff.

## Ratified preflight rulings — controller 2026-09-22

- **Ruling: P01 — exact persisted names.** Use design names verbatim: `StartImport/v1/...` and `Retry/v1/...` despite global `v1/...` wording; omit `root` from issue-page Activity names, retain issue ID for comments, and make restart tests use the complete helper-produced name including hash/try. Do not silently rename persisted identities later. Cost if wrong: incompatible workflow history and replay failures.
- **Ruling: P02 — Task 1 examples are sketches, not runnable contracts.** Define schemas before `Workflow.make`; wrap start/finalize external effects in real durable Activities; increment/assert the actual fake side effect; use command-derived migration ID and scan revision (rescan success preserves public migration ID). Memory replay tests retain one engine/history while resuming; recreating `layerMemory` loses its local Maps and cannot demonstrate persistence. Cost if wrong: false restart assurance or broken rescan identity.
- **Ruling: P03 — encode every persisted failure.** A `Data.TaggedError` client error may remain internal but must map through a named Effect Schema at Activity boundaries (or itself become schema-backed); `Exit<unknown, unknown>` is acceptable only as an in-process finalizer callback, never a persisted result. Preserve existing shared public error taxonomy. Cost if wrong: undecodable durable history or accidental HTTP widening.
- **Ruling: P04 — safe staged schema and visibility work.** Add nullable `publishedAt` without an initial non-null default, backfill exact `createdAt`, then add normal-insert default; explicitly write `now` for ordinary projects. Include route/MCP tests and actual public consumer inventory even when not fully named in Files. New fence columns must account for existing PR migration rows without pretending they already have workflow executions. Cost if wrong: changed historical timestamps, hidden-row leak, or migration failure. **Approval boundary:** any policy to resume/adopt/discard existing legacy worker jobs is not specified; ask Wouter if production data requires such a policy, rather than inventing it.
- **Ruling: P05 — finish the projection interface once.** T3 owns all listed projection methods and row/detail mapping; T7/T10 may add required finalization functions in that same module even where omitted from their Files lists. `owned` remains an internal row read; public commands call `toDetail`; handler tests must be staged in the commit. Preserve explicit CAS predicates for revision, current execution/attempt, status, and cleanup claim as applicable. Cost if wrong: interface churn or stale writes.
- **Ruling: P06 — drafts stay inside the existing HTTP schema.** Incomplete arrays are accepted, semantically incomplete choices produce `needs_configuration`, and invalid destination/field shapes remain ordinary validation failures. Do not widen `JiraMigrationConfiguration` to make arbitrary malformed form states persist. Cost if wrong: some invalid edits cannot survive navigation. **Approval boundary:** if Wouter expects invalid destination fields or absent restriction policy to persist as drafts, the current non-empty/enum schema cannot represent that; shared API/schema changes require his choice.
- **Ruling: P07 — page results and nested concurrency.** Preserve the declared refs/count/cursor/warnings Activity result; derive issue IDs from the normalized chunk rather than reading nonexistent `page.issueIds`. Apply bounded concurrency around real dependent collection page execution, not merely iteration of IDs. Detect repeated cursor and stalled offset progression with request coordinates. Cost if wrong: missing nested data, unbounded requests, or infinite scan.
- **Ruling: P08 — cleanup commands need a recoverable start handshake.** Carry the explicit cleanup command interface from T7 to T11; deterministically derive target generation from the expected next CAS revision and use the already-approved execute-first/idempotent-CAS pattern for both caller and cleanup start Activity, avoiding an unrecoverable claimed row before enqueue. Lost races exit without side effects; retry/reset/discard share status/retention/claim checks; reset waits for verified removal before installing/starting the new scan and must block old publication. Cost if wrong: orphan claim or deletion racing resumed work. **Controller scrutiny:** this applies the approved handshake pattern to an underspecified cleanup boundary; if a different persistence/data-flow design is needed, stop for Wouter rather than invent it.
- **Ruling: P09 — retain intermediate compatibility, never dual execution.** Earlier tasks may retain old worker type surfaces until T11 because the plan requires backend typechecks before cutover. Use versioned adapters where needed; do not run old worker and new workflow against the same new migration. Keep intermediate tests injectable and production release gated until T11. Cost if wrong: intermediate compile breakage or competing executors. This proposal belongs to T1/T3/T4/T5/T6/T8/T11 even where individual table rows reference the narrower compatibility proposals.
- **Ruling: P10 — freeze complete publication contract before consumers.** T8’s schema must include every prose-required map/document/index row and version, not just the shortened example. Define `publicationRevision` from an accepted immutable publication plan and preserve it across retries; T9’s extra hidden/report/verify/batch identities need explicit, versioned names before persisting. Keep planned skips distinct from copy failures; unresolved copy failures cannot silently mutate the frozen plan or publish unexpected rows. Cost if wrong: inconsistent materialization and replay. **Approval boundary:** spec does not choose the source of publicationRevision, new batch-name exact forms, or a plan-regeneration protocol for newly accepted partial outcomes; if the controller cannot derive these as implementation details inside the approved plan, obtain Wouter’s choice before those tasks. No helper-selected protocol is ratified here.
- **Ruling: P11 — manifest compatibility is explicit.** Add manifest v2 as a named schema; do not silently reinterpret v1 stored artifacts or rewrite old history. Retain v1 decoding/type compatibility only as required by still-present PR code until its consumers migrate; corrupt/unsupported versions surface as typed failures. Cost if wrong: unreadable diagnostics or broken intermediate consumers. Adoption of in-flight legacy migrations is P04’s separate approval boundary.
- **Ruling: P12 — keep storage and history boundaries exact.** Bulk source JSON/ADF, Markdown, reports, archives and publication plans stay in organization storage; Activity results carry only the defined small references/counters/warnings. Artifact keys refer to logical work, so operationTry does not create duplicate permanent identities. `listObjectKeys` may return the planned aggregate, but deletion concurrency remains bounded and every token/cursor validated. Cost if wrong: history bloat, duplicate artifacts or private-content exposure.
- **Ruling: P13 — one logical client operation may refresh authorization once.** The one-request assertion excludes the explicitly allowed 401 forced-refresh replay; no pagination/transient/429 retry loops belong to the client. Preserve selection aggregate APIs using Stream pagination, accept only valid Retry-After up to exactly 86,400,000 ms, and map invalid input to typed transient failure. Fix the six existing Client.test.ts Effect-provision warnings in this task. Cost if wrong: reconnect or retry regression.
- **Ruling: P14 — one bounded retry loop per logical unit.** Treat recursive sample code as control-flow illustration; avoid outer catch handlers recatching errors from recursively resumed work and incrementing multiple failure sequences. Each exhaustion/reconnect records one fenced generation, awaits its exact token, then retries with a new operationTry. The same design applies to storage/Markdown errors, not only JiraTransientFailure. Cost if wrong: infinite waits, repeated user prompts or skipped work.
- **Ruling: P15 — commands return current truth.** After bounded signal observation timeout, reread the latest projection instead of returning the stale captured `currentDetail`; timeout must not cancel durable work. Retry claims/clears retention atomically before signal; cancellation finalization cannot overwrite success. T7 may use explicit fakes for not-yet-implemented publication/cleanup, but T10/T11 must provide real race evidence. Cost if wrong: stale UI or lost cleanup/retry race.
- **Ruling: P16 — behavioral domain tests and privacy over abbreviated examples.** Keep deterministic same-input/different-ID assertions but also prove fixed expected identity/schema compatibility, no filename/size identity influence, byte-stable ordering, and actual whole-archive exclusion. Include unsupported/ref/partial-outcome categories; use existing palette behavior in old status tests rather than changing approved colors for stale expectations. Cost if wrong: nominally passing tests miss unstable IDs or excluded data leaks.
- **Ruling: P17 — fence database writes atomically.** An unfenced hidden insert followed by verification does not meet the design: check ownership/fence and perform relational writes in one appropriate transaction/CAS. External writes use deterministic private identities, bounded execution, and pre/post ownership checks; cleanup must wait for/coordinate interruption so late writes cannot recreate removed objects. All public seams remain hidden until commit. Cost if wrong: orphan rows, artifact resurrection or accidental publication.
- **Ruling: P18 — one publication transaction and replay-safe success.** Choose one clear transaction owner consistent with existing Db API; pure builders accept supplied data and publication inserts only through that transaction handle. Recognize already-committed success for the same fence/plan as idempotent completion if Activity-result recording was lost; never fail merely because `publishedAt` is now non-null. Conditional finalization/row locking must make cancel-before-publication and publication-before-cancel coherent. Cost if wrong: visible-but-cancelled state or false failed replay.
- **Ruling: P19 — prove abrupt restart and qualify no-duplicate guarantee.** T11 must kill an isolated backend process/runtime without graceful workflow completion, then start a fresh runtime over the same disposable SQL/storage; graceful `close` alone is not the design’s proof. Assert exactly one call for completed, persisted page Activities; a Jira read killed before its result/artifact is durably recorded may be repeated, since Jira cannot offer transactional read/result persistence. Deterministic writes must converge in that window. Cost if wrong: false durability claim. Controller should explicitly ratify the latter clarification against Review Focus 1; a literal zero-repeat guarantee for every unfinished HTTP read is not implementable with this stack.
- **Ruling: P20 — remove migration execution leases, preserve OAuth coordination.** Scope searches to worker/projection/import execution fields and exclude actual historical migration paths. `Credentials.ts` contains legitimate `leaseId`/`leaseExpiresAt` locals for encrypted OAuth refresh coordination; the blanket zero-match command must not cause their removal. Cost if wrong: duplicate token refresh or unnecessary credential redesign.
- **Ruling: P21 — production cleanup/runtime completion.** Provide the real SqlClient and BunCrypto to a single memoized SQL SingleRunner/engine used by registrations and commands; skeletal Layer sample does not remove those requirements. Post-success failure never downgrades migration and must retain a concrete retry path; retention only schedules. Delete projection last, clear failed cleanup claim with generation fence, preserve reset diagnostics and permanent published data. Cost if wrong: stuck cleanup, duplicate runners, missing services or data loss. If durable automatic cleanup retry requires a new orchestration design beyond bounded Activity retries/new cleanup generation, controller must ask Wouter.
- **Ruling: P22 — draft/navigation and motion behavior.** Serialize step/leave saves using existing form/atom conventions, read live form values, accept new revision after each save, and prevent navigation on failed or timed-out save. Keep parent-owned DraftSaveRef; use existing router navigation blocking if available to cover supported route exits, and do not claim asynchronous persistence is guaranteed on browser hard close. Use a combined transition property list or existing `transition-all` so color and transform both transition; two conflicting Tailwind transition-property utilities are insufficient. Read the project UI quality checklist if located; no checklist file was found by filename search, so explicitly review PRODUCT/DESIGN rules. Cost if wrong: lost drafts or broken hover feel. New navigation data-flow/library or broader unload guarantees require Wouter, not a helper-designed mechanism.
- **Ruling: P23 — isolate harness resources before side effects.** Validate both required DB/bucket prefixes and local/loopback host configuration before migrations, data resets, bucket creation or OAuth redirects; refuse inherited remote credentials/endpoints. Use production API/credential flow with existing injectable transport/token seams; scenario controls exist only in standalone loopback harness. Test actual validator Result shape rather than copy the unbound `Left` example. Cost if wrong: preview/resource mutation or misleading fixture tests. A missing injection seam may be extended only within the approved harness boundaries; new dependencies/shared HTTP API require Wouter.
- **Ruling: P24 — final release verification scope.** Treat Task13 as backend harness and Task14 as browser walkthrough despite global task-number typo. Run one final browser walkthrough after final code/test fixes; repeat only after new changes/failures, not solely because Step5 duplicates Step2/3. SQL suites must demonstrably execute, never pass via skips. Delete only the exact six confirmed PR-owned images. Cost if wrong: inadequate release evidence or unrelated file loss.
- **Ruling: P25 — review and commit evidence.** File lists and git-add examples are incomplete in places; stage every intentional owned test/change and no unrelated file. Record command/output evidence in task reports, baseline warnings separately, and all cannot-verify items against downstream gates. A passing mock-only command test is not a substitute for SQL/public-seam evidence. Cost if wrong: unreviewed/uncommitted test files or unsupported completion claim.

## Review-rubric conflict scan

| Mandate/example | Rubric risk | Proposed resolution |
| --- | --- | --- |
| T1 replay counter created but not wired in sample | Test may assert incidental mock state rather than actual durable Activity behavior. | P02: real callback counter and effect execution under engine; SQL proof later. |
| T1 callbacks shown without Activity construction | Claimed durable side effect is merely an Effect call. | P02/P03: actual Activity wrapper and schema-backed outcomes. |
| T3/T7 action races before real cleanup/publication | Mock-only race proof could claim real transaction correctness. | P15/P18/P25: scoped command tests plus explicit downstream SQL gate. |
| T8 identity same-input assertion | Repeatability alone passes a constant implementation. | P16: distinct identities, deterministic expected bytes, valid shared Attachment ID, irrelevant-name/size independence. |
| T9 insert then verify | Wrong/stale write already exists before failed verification. | P17: atomic DB fence check and write. |
| T10 requires unpublished on every publish attempt | Successful commit before result persistence would fail replay. | P18: matching committed outcome is success. |
| T11 `firstRuntime.close` restart sample | Graceful scope completion is not process death evidence. | P19: genuinely abrupt isolated process termination. |
| T11/T14 zero matches for any lease variable | Removes unrelated OAuth correctness mechanism mandated to remain. | P20: scope migration-worker search. |
| T12 `transition-colors transition-transform` | Last transition-property utility replaces earlier property set. | P22: combined properties/shared primitive behavior. |
| T14 duplicate browser pass with no change | Verification-only repetition without new risk conflicts with developer test guidance. | P24: final post-change pass and explicit evidence. |
| Plan code snippets omit methods/fields/add paths | Literal transcription fails prose-required behavior or leaves files uncommitted. | P05/P10/P25: prose/spec requirements govern complete implementation. |

No verbatim duplicated production logic is mandated; reusable pure index builders and existing projection/artifact seams should avoid it. No empty/assertion-free test is explicitly required, but sample harness placeholders must become real behavioral assertions rather than test-only simulations of the result being claimed.

## Architecture decisions requiring Wouter if encountered

There is **no unconditional new-library/top-level-module/HTTP-widening decision required before Task 1**: the migration design and supplied plan already select their architecture. Stop candidates that cannot be silently chosen are legacy in-flight-job adoption policy (P04), persisting drafts outside current schema (P06), any genuinely new publication-plan/batch naming or outcome regeneration protocol not derivable from approved contract (P10), new cleanup retry orchestration beyond approved workflow mechanisms (P21), or new navigation/library/API design (P22/P23). These remain unchosen; controller ratification of implementation-level corrections is not approval to cross those boundaries.

## Operational ruling/evidence supplied by controller

- **Ruling:** use new isolated `projectproject-t172-local-postgres` (`postgres:16-alpine`) at loopback port `55432`, because unrelated `omgevingschat-postgres` occupies `5432`. Test URL: `postgres://projectproject:projectproject_dev@127.0.0.1:55432/projectproject_effect_v4_t172`; browser DB retains exact `projectproject_effect_v4_t172_browser` name on that isolated port. No shared container or preview resource touched. Cost if wrong: local command examples require port/container adaptation; fixture scope must remain isolated.
- Disposable SQL baseline: backend `--no-file-parallelism`: 72 files passed / 1 failed; 786 tests passed / 1 failed / 5 skipped. Existing `Jira/Migrations.test.ts:323` expects color `#22c55e`, actual `#f16f7e`. Carry correction into T3/T8 test modernization; preserve approved palette. Evidence: `/tmp/t172-baseline-backend-sql-tests.log`.

## Controller ratification and task execution log

Controller ratified P01–P25 as spec-derived implementation corrections, with P24 qualification below. No authority to introduce architecture outside the spec/plan is delegated.

- Ruling: P24 qualification — invoke browser:control-in-app-browser in both Tasks 13 and 14 as explicitly requested; Task 13 verifies the local harness browser connection/public surface and Task 14 performs the full final walkthrough after focused tests. Repeat scenarios only after changes or unresolved failures — preserves explicit skill use and meaningful final evidence — cost if wrong: another browser verification pass is needed.
- Ruling: baseline failures are recorded and assigned to planned owning tasks rather than blocking unrelated implementation — Execution Baseline explicitly instructs recording them before code changes, and user requires continuous execution — cost if wrong: later quality gate may expose additional baseline remediation.
- Task 1: dispatched; base 4d06422f; implementer /root/task_01_implementer; model gpt-5.6-sol/high; fresh context; sequential implementation; review pending.


- Local storage provisioned: isolated container projectproject-t172-local-minio, loopback S3 59000 and console 59001, disposable container-local storage. Browser bucket remains projectproject-t172-local-browser; no buckets created or preview credentials copied.

- Task 1: implementation reported DONE, commit 9051e59e; focused 5/5; repo 1548 pass/65 skipped; lint/format pass; backend typecheck identical to baseline. Task review dispatched /root/task_01_reviewer, model gpt-5.6-sol/high, package review-4d06422f..9051e59e.diff; verdict pending.

- Task 1: review verdict spec FAIL / quality Needs fixes. Important: manual resume test resumes running fiber, a no-op. No minors. SQL cannot-verify resolved by explicit Task11 release gate; test rerun note requires no action.
- Task 1: fix round 1/5 dispatched to original /root/task_01_implementer (gpt-5.6-sol/high); fix base 9051e59e; covering test MigrationWorkflow.test.ts; pending fix and scoped re-review.

- Task 1: fix commit 3d23bd5a; fix report includes covering MigrationWorkflow.test.ts command and 5/5 passing output. Scoped re-review /root/task_01_rereview_1, gpt-5.6-sol/medium, package review-9051e59e..3d23bd5a.diff, pending.

- Controller resumed after user status interruption. Verified clean HEAD 3d23bd5a and ledger; no tasks reimplemented. Prior scoped reviewer no longer live and no verdict returned; resumed only pending re-review as /root/task_01_rereview_1_resume, gpt-5.6-sol/medium, same fix range/report.

- Task 1: fix round 1/5 (1 addressed, 0 open — actual suspended resume verified; commits 9051e59e..3d23bd5a); independent scoped verdict all addressed, no new breakage or minors.
- Task 1: complete (commits 4d06422f..3d23bd5a, review clean)
- Task 2: dispatched; base 3d23bd5a; fresh implementer /root/task_02_implementer, gpt-5.6-terra/high; visibility/schema integration; review pending.

- Task 2: implementation DONE, commit 2c0d4034; focused isolated SQL 6 files/109 tests pass; full suite 1549 pass/67 skipped; typecheck baseline diagnostics remain. Review /root/task_02_reviewer, gpt-6-astra/high, chosen for multi-surface visibility/auth risk; package review-3d23bd5a..2c0d4034.diff; verdict pending.

- Task 2: review spec FAIL / quality Needs fixes; 3 Important findings: admin serving fallbacks, org attachment library/summary/delete visibility, missing public ticket-route proof. No new minors. Internal hidden lookup cannot-verify resolved by controller reading actual cleanup ID selection (Migrations.ts:409–437) plus SQL read fixture; populated upgrade evidence gap added to fix.
- Task 2: fix round 1/5 dispatched original /root/task_02_implementer gpt-5.6-terra/high; fix base 2c0d4034; covering projectVisibility.test.ts, Attachments.test.ts, FigmaLinks.test.ts and focused HTTP ticket/attachment/thumbnail seams.

- Task 2: fix commit 05ebd2f0; behavioral RED demonstrates hidden signing/library leaks, GREEN 3 files/139 tests including real TicketsLive HTTP request and upgrade evidence. Scoped re-review resumed independent /root/task_02_reviewer (gpt-6-astra/high), fix package review-2c0d4034..05ebd2f0.diff. Implementer separately retrieving already-launched root-suite result; no further implementation dispatched.

- Task 2: post-fix root-suite result unavailable: implementer retained neither exit/output nor session ID; process check found no active runner. Do not claim this run passed. Required fix-covering suite has complete 139-pass evidence; next task and final release run full suite afresh. Report-only follow-up finished, no code changes.

- Task 2: fix round 1/5 (3 original findings addressed, 2 open — new orphan-serving regression and actual upgrade evidence gap; commits 2c0d4034..05ebd2f0). Scoped reviewer gpt-6-astra/high; no deferred minors.
- Task 2: fix round 2/5 dispatched original implementer gpt-5.6-terra/high, base05ebd2f0; cover db/projectVisibility.test.ts and Layers/Attachments.test.ts; reviewer findings verbatim in task-2-rereview-1.md.

- Task 2: fix round2 commit dc023159; 2 files/95 covering tests pass; actual migration-file upgrade tested in rolled-back temp schema. Independent scoped reviewer /root/task_02_reviewer gpt-6-astra/high resumed with review-05ebd2f0..dc023159.diff; verdict pending.

- Task 2: fix round 2/5 (2 addressed, 0 open — orphan serving restored; actual populated migration-file upgrade verified; commits 05ebd2f0..dc023159).
- Task 2: minor (deferred): db/projectVisibility.test.ts:499 summary count3 depends on orphan inserted in preceding test; isolated library-test execution fails. Final whole-branch review must triage.
- Task 2: complete (commits 3d23bd5a..dc023159, review clean except deferred minor)
- Task 3: dispatched; base dc023159; fresh implementer /root/task_03_implementer, gpt-6-astra/high for durable handshake/CAS concurrency; review pending.

- Task 3: NEEDS_CONTEXT / architectural stop before production or test edits; base remains dc023159; no commit. Evidence task-3-report.md: conflicting-source create may persist workflow payload A while handler B inserts projection B; public Workflow API exposes no accepted-payload lookup. Intermediate production layer also lacks engine and old worker would claim new workflow rows.
- Pending Wouter decision (not a ruling): recommend sole workflow-start projection insertion with bounded handler observation/full-source comparison; alternative durable accepted-source acknowledgment adds a persisted protocol. Recommend separate injectable workflow-backed service composition until approved Task11 production cutover; alternative moving runtime cutover earlier changes sequencing. Neither proposal implemented.
- Resume at Task3 after architecture approval; original implementer /root/task_03_implementer gpt-6-astra/high may be resumed with recorded choice. Tasks1–2 complete, Tasks3–14 pending. HEAD dc0231591d276fb7133c89e28d4069189827f2f7, clean worktree. No push/merge/publish. Disposable local Postgres55432 and MinIO59000 remain for continuation; preview untouched.

- User decision: Wouter approved both pending architecture proposals (“yeah sounds good.”): workflow-only create projection insertion with bounded handler observation; separately testable workflow-backed service until Task11 production cutover. Full amendment task-3-approved-amendment.md. Resume original Task3 implementer gpt-6-astra/high at unchanged base dc023159; update authoritative design/plan in task commit. No task redispatch or repeated completed work.

- Task 3 boundary clarified within user-approved staging: existing JiraMigrationsLive remains production legacy; JiraMigrationsWorkflowLive is isolated workflow-backed composition for tests and Task11 cutover. All eight signatures retained; Task7 still owns durable run/retry/cancel/discard orchestration and Task11 cleanup. Task3 may reuse safe existing command helpers with fenced new-row transitions, but may not create a throwaway worker/cleanup protocol or claim downstream integration complete.

- Ruling: Task3 separately testable workflow composition requires constructor-injected run/cancel/discard operations (existing eight-method signatures); no default/no-op/unwired-error fallback or legacy discard for workflow rows — within user-approved isolated composition, avoids legacy cleanup deleting recovery projection after swallowed error; Task7 supplies real durable commands before Task11 cutover — cost if wrong: internal factory wiring must be revised, no public API/history change. Projection cleanup claim/release/delete-last fences remain Task3 deliverables.

- Task 3: second architectural stop, actual WorkflowEngine.layerMemory + SQL regression proves rejected rescan execution reuse. At revision R an enqueued next-attempt workflow loses to configuration revision R+1 and completes Conflict; rescan from R+1 computes the same migrationId:attempt key and installs that completed execution as scanning. Evidence task-3-rescan-race-red.log; no identity fix made.
- Pending Wouter decision: recommend including expectedRevision in the rescan idempotency key, retaining same-revision convergence and fencing; alternative attempt-allocation redesign is larger. Required by explicit AGENTS.md architecture approval and design lines160–166 fixed persisted key. Previous approval covers create handshake and isolated composition only. Task3 remains uncommitted/incomplete, independent reviewer not yet dispatched.

- User authorization: Wouter explicitly instructed “yeah please keep going autonomously, make the descisions yourself. I am not here to answer questions..”. This approves the proposed rescan revision key and delegates further architectural/implementation decisions within this migration scope to the controller, overriding earlier ask-first instructions. Preserve all substantive scope constraints and no push/merge/publication/shared branch changes.
- Ruling: rescan identity becomes migrationId:workflowAttempt:expectedRevision — prevents reuse of a workflow whose authoritative start CAS failed at an earlier projection revision, while same-revision calls converge — cost if wrong: persisted identity compatibility; new isolated workflow is not deployed, so amend spec/plan/contracts before production cutover.
- Task3 recovery: original implementer no longer available after user-turn transition; preserve uncommitted work and regression, dispatch replacement solely to finish Task3 (not a new parallel implementation). Base remains dc023159.

- Task3 resumed as /root/task_03_resume, gpt-6-astra/high (original agent unavailable), existing edits preserved. Approved revision-key regression and same-revision convergence now green: 26 tests across5files. Broad checks in progress; no commit/review verdict yet.

- Task3 implementation DONE_WITH_CONCERNS, a23ac11f; 26 focused /1551 root /806 isolated SQL tests passed; baseline six Client warnings only, typecheck exit1. Independent task review /root/task_03_reviewer gpt-6-astra/high for concurrency correctness; review package dc023159..a23ac11f, verdict pending. Module size852lines noted for reviewer.

- Task3 review: spec FAIL / quality Needs fixes; Important predecessor execution identity lost on beginRescan replay, so crash after CAS can leave old execution suspended forever. Review task-3-review.md.
- Ruling: persist supersededExecutionId in the Rescan command, obtained from the owned row before enqueue; validate predecessor with initial CAS and return the command predecessor on exact-target replay, so the durable start retries idempotent interruption — smallest durable repair within user-delegated architecture authority — cost if wrong: new undeployed v1 payload changes require coordinated test/docs updates; no live history migration.
- Task3 cannot-verify resolved: production cutover/commands/publication/cleanup remain explicitly assigned T6/T7/T10/T11; T7/T10 must add fenced cancelling→cancelled finalization (current transition excludes cancelling), required downstream acceptance not claimed by T3.
- Task3 minor (deferred): known jsdom canvas/SQLite output and baseline six Effect warnings; T5 owns Client warnings, final verification must distinguish warnings from failures.
- Task3 fix round1/5 dispatched /root/task_03_resume gpt-6-astra/high, base a23ac11f; covering MigrationWorkflow.test.ts, Migrations.test.ts, MigrationProjection.test.ts.

- Task3 fix1 commit43e1bf75; 27 focused tests passed, baseline typecheck warnings only. Scoped independent re-review /root/task_03_reviewer gpt-6-astra/high resumed against a23ac11f..43e1bf75; verdict pending.

- Task3 fix round1/5 (1 addressed,0open — predecessor persists/replays interruption; commits a23ac11f..43e1bf75), independent scoped verdict clean.
- Task 3: complete (commits dc023159..43e1bf75, review clean; baseline noise deferred).
- Task4 dispatched; base43e1bf75; fresh implementer task_04_implementer, gpt-5.6-sol/high for artifact/schema/storage integration.

- Ruling: Task4 private artifact service uses orgSlug-first methods plus OrgStorage/S3Storage; artifactKey/ref.key stay logical, service applies normalized connection.keyPrefix exactly once, listing returns logical scoped keys — actual S3Storage uses literal keys and does not apply prefix — cost if wrong: internal callsites need adjustment; no public API change. Require nonempty-prefix coverage.
- Ruling: Task4 retains explicit JiraMigrationManifestV1 plus existing legacy alias, adds JiraMigrationManifestV2 and version constant2; v2 explicit category arrays/rawArtifacts refs/schema+converter versions, stable-ID normalized — Task8 already names V2 and legacy consumers remain staged — cost if wrong: downstream schema adjustment before persisted production use.

- Task4 RED captured task-4-red.log: new artifact module/manifestV2/normalizer/listObjectKeys absent, three files failed; existing26S3tests passed. Tee initially masked shell exit, runner reported exit1; implementer instructed by own checkpoint to use pipefail for final evidence.

- Task4 implementation DONE_WITH_CONCERNS, commit1418e791;36focused/1561repositorytests pass; sixbaselineClientwarningsonly, typecheckexit1. Independent reviewer /root/task_04_reviewer gpt-5.6-sol/high for bounded storage/schema change; package43e1bf75..1418e791; verdictpending.

- Task4 reviewer spec PASS / quality Approved, no Critical/Important. Cannot-verify real Activity result payload secrecy resolved as explicit T6 scan and T9 import gates (artifact service itself has exact reference/marker proof). Minor deferred: existing typecheck/environment output noise, already tracked T5/final.
- Task 4: complete (commits43e1bf75..1418e791, review clean except baseline noise).
- Task5 dispatched; base1418e791; fresh implementer /root/task_05_implementer gpt-6-astra/high chosen for credential/error-boundary and pagination integration complexity; sequential implementation.

- Ruling: Task5 new page methods accept one object (userId/cloudId/resource/cursor coordinates), existing aggregate positional APIs remain; private Schema-backed errors live in Blocked.ts, credential-origin public429 maps inward and HTTP seams map outward to unchanged shared seconds/error taxonomy — preserves public contract while enabling durable Activity errors — cost if wrong: internal callsite adjustments; require boundary encoding tests.

- Ruling: Task5 accepts IMF-fixdate, RFC850 and asctime HTTP-date forms with strict grammar/calendar checks, Effect Clock and same max delay; RFC850 >50-year future interpretation follows RFC9110§5.6.7 https://www.rfc-editor.org/rfc/rfc9110.html#name-date-time-formats — verified standard requires all three, matching plan valid-HTTP-date promise — cost if wrong: parser complexity; reuse common validation and focused tests, no dependency.

- Task5 implementation DONE, commit5eecde40;78focused+4isolatedcredential tests pass; repository1625pass/84skip before final attachment fixes (covered by78focused afterward). Backend typecheck exit0, scoped lint/formatclean. Independent reviewer /root/task_05_reviewer gpt-6-astra/high for credential/paging/error-boundary risk; package1418e791..5eecde40, verdictpending.

- Task5 review spec FAIL / quality Needs fixes: Important JSON body acquisition network errors collapsed to JiraError(invalid_response), preventing downstream retry; reproduced with real Fetch/Jira transport stream socket reset. task-5-review.md.
- Task5 fix round1/5 to original /root/task_05_implementer gpt-6-astra/high, base5eecde40. Cover Client.test.ts real transport/body failures and MigrationHandlers.test.ts public mapping. Separate body acquisition failures from JSON syntax/schema failures; keep one logical request/no sleeps.
- Task5 minor (deferred): new Schema decoder suggestions Client.test.ts:1160,1164 and Client.ts:405; baseline SQLite/jsdom noise.
- Task5 cannot-verify resolved: credential files unchanged, isolated real DB Credentials.test.ts4/4 proves current token persistence/lease paths; durable Activity integration explicitly Task6, not claimed here.

- Task5 fix1 commit2c78bfdf;83focusedGREEN after2expectedRED, backendtypecheck/lint/formatpass. Independent scoped re-review /root/task_05_reviewer gpt-6-astra/high resumed against5eecde40..2c78bfdf, verdictpending. Report corrects earlier overstatement: new suggestion-level diagnostics exist, no warnings/errors; minors deferred, exit0 remains verified.

- Task5 fix round1/5 (1addressed,0open — network body acquisition distinct from malformed JSON;5eecde40..2c78bfdf), independent scoped review clean.
- Task 5: complete (commits1418e791..2c78bfdf, review clean except deferred Minor suggestions).
- Task6 dispatched; base2c78bfdf; fresh implementer /root/task_06_implementer gpt-6-astra/high for durable replay/retry/concurrency orchestration.

- Ruling: Task6 adds required scan callback to MigrationActivities, explicit real/test implementations at every callsite — avoids no-op production fallbacks — cost if wrong: internal fixture/factory signature edits.
- Ruling: Task6 recordScanFailure returns accepted/current failure generation; resumeScan(fence,sequence) CAS is replay-idempotent. Record generation in an Activity result, converge concurrent pre-retry failures, never let old generation clear new failure/cleanup ownership; retain existing boolean finalizer method — enables shared generation-specific retry without duplicate sequence increments — cost if wrong: stuck or duplicate retry gates; real concurrency/replay tests required.
- Ruling: correct undeployed ManifestV2 attachment field contentArtifact to metadataArtifact, plus explicit nullable downloadUrl/jiraUrl and downloadAllowed. Scan captures metadata, not binary content; binary refs belong later materialization/publication output — cost if wrong: v2 consumers/tests require coordinated change; no production v2 histories exist. Never relabel metadata bytes as attachment content.
- Ruling: Task6 may add Jira/ScanV2.ts for pure v2 normalization/validation while keeping legacy Scan.ts builder until cutover; update task Files/docs/brief — avoids growing legacy600line module with another350lines — cost if wrong: one internal file boundary, no new package/public API.

- Task7 preflight follow-up recorded in task-7-context.md: evaluate process death between accepting run/retry row CAS and deferred completion; do not rely solely on uninterruptibility or signal-first stale-token poisoning. Controller will resolve concrete protocol with implementer under delegated authority before committing; no architecture choice ratified yet.

- Read-only Task7 protocol preflight helper /root/task_07_protocol_preflight gpt-6-astra/high dispatched during sequential Task6 implementation. Research only (not task implementer/reviewer), no code changes/subagents. Goal: concrete public-Effect solution for CAS/deferred process-crash gap; controller decides; output task-7-protocol-preflight.md.

- Ruling: Task6 corrects Task5 private page boundary to retain exact raw JSON before decoding via raw field on cursor/offset results; scanner-needed direct metadata gets narrow typed {raw,value} snapshot variants sharing request/decode implementation, existing decoded/aggregate APIs remain — decoded-only responses discard unmodeled fields and violate authoritative provenance requirement — cost if wrong: internal client shape/fixture updates; never persist raw in Activity result/history. Add unknown envelope/nested-field preservation and real Activity secrecy regressions; update owned Files/docs.

- Ruling: Task6 records private scanFailureReceipts mapping full logical-unit/operationTry→failureSequence in projection checkpoint, atomically with status/generation under row lock. Existing receipt returns same generation after resume; every progress/checkpoint merge preserves receipts; clear only new scan revision — closes Activity reply-loss race without relying on early-delivery transaction annotation semantics — cost if wrong: private checkpoint growth/merge complexity; require replay-after-retry and concurrent merge tests. It stores idempotency metadata, never schedules/claims execution.
- Ruling: Task7 uses one shared PgClient transaction around domain revision/fence CAS and public DurableDeferred.succeed; observation occurs after commit. Installed Effect SQL engine persists deferred request in sender transaction and wakes storage reader, so rollback removes both state change and signal. No extra command Workflow, table/column, or custom outbox. Evidence task-7-protocol-preflight.md — cost if wrong: atomicity depends on same memoized PgClient and SQL engine; mandatory real SQL rollback/no-premature-delivery regression in Task7, abrupt-process proof in Task11. Memory engine tests cannot prove this boundary.
- Task7 read-only preflight complete; helper /root/task_07_protocol_preflight gpt-6-astra/high made no implementation edits. All recommendations checked; WithTransaction Activity alternative rejected for this need due unnecessary orchestration and unresolved early-result delivery caveat.

- Read-only publication protocol preflight /root/publication_protocol_preflight gpt-6-astra/high dispatched while Task6 implements. Two concrete P10 integration gaps: configuration-blocked return needs fresh signal despite fixed consumed StartImport token; immutable final plan must incorporate actual partial attachment outcomes known later. Research only/no code/subagents, controller will rule; output publication-protocol-preflight.md.

- Ruling: Task6 named optional endpoint allowlist (watchers/votes/boards/boardConfiguration/sprints and issue comments/changelogs/worklogs) may map403/404 to explicit partial/unknown coverage, coded permission/missing warnings and safe diagnostic artifacts. Never infer complete coverage from empty unavailable data. Project/issues and mapping-required metadata access remain blocking; reconnect/rate/transient/malformed/storage failures retain proper failure channels — spec captures when visible and requires inaccessible-data caveats — cost if wrong: optional unavailable source data omitted with explicit report, never silent; regression plus core blocking control required.

- Ruling: Task6 metadata Activity names use coordinate form v1/scan/{kind}/{scanRevision}/{pageOrdinal}/{cursorHash}/{operationTry}, not bare fixed name — explicit retries must not replay cached failures; issue/comment forms retained — cost if wrong: undeployed name-contract amendment, update spec/plan/tests before production.

- Ruling: publication-protocol-rulings.md ratifies current-gate routing (initial StartImport, corrected-ready Retry), immutable accepted preparation then settled-copy final plan, sha256 publicationRevision outside payload, skip-only late configuration for explicitly failed IDs, exact retry-aware Activity names. Why: fixed consumed tokens and pre-copy immutable documents cannot handle required corrections/partial outcomes. Cost: coordinated internal contracts/tests before deployment; no public API widening/new dependency/execution system. Full choices and task ownership in that artifact; read-only helper complete with no code edits.

- Ruling: Task6 adds private scanPages receipts keyed by full logical page coordinates within the scan revision, atomically merged with failure receipts and cleared only for rescan — avoids duplicate progress when projection writes commit before Activity reply persistence, preserving concurrent page counts — cost if wrong: checkpoint growth/merge complexity; real SQL duplicate/replay and concurrent count tests required. This remains projection idempotency metadata, never execution ownership or scheduling.

- Ruling: Task6 completeScan replay accepts same-fence scanned needs_configuration/ready/migrating only when persisted scan artifact identity/checksum matches, with no mutation; resumeScan exact-generation replay may likewise accept those scanned states without rolling back to scanning. Reject cleanup/cancelling/terminal/newer generation — user may configure/run after scan projection commit but before Activity reply persistence — cost if wrong: stale scan may overwrite accepted import state; SQL post-run replay, mismatched scan identity and newer-generation controls required. Task7 chooses scanning versus migrating from current gate/phase.

- Ruling: Task6 snapshots identity mapping options through a durable metadata Activity using existing coordinate naming with kind identity-options and returns only an artifact reference; manifest/requirements build reads the checksum-verified snapshot, sorted chunk refs and stable raw-key warning IDs — live user-list or concurrent completion order changes must not rewrite artifacts on reply-loss replay — cost if wrong: an additional small private artifact/Activity and frozen identity option snapshot; require changed-provider replay and stable requirements-byte coverage.

- Task6 implementation checkpoint: no-DB full suite1653pass/86skip before final bounded replay fixes; latest covering SQLprojection8/8 and scan/workflow23/23 pass, including accepted migrating state, checksum mismatch, null checkpoint merge, stable identity snapshot and clock-advance requirements bytes. Final focused/typecheck/report/commit pending; no review verdict yet.

- Task6 implementation DONE_WITH_CONCERNS commit8326051c;122focused +23finaldeterminism/name +8finalSQLprojection pass, typecheck/lint/format exit0;1653rootpass before bounded final fixes, covered separately. Independent reviewer /root/task_06_reviewer gpt-6-astra/high for durable concurrency/provenance risk; package2c78bfdf..8326051c, verdictpending. Production SQL restart/command integration explicitly T7/T11, no completion claim for those.

- Task6 independent review spec FAIL / quality Needs fixes: Important concurrent generation1 resume after sibling generation2 causes false terminal supersession; Important ordinary epic children classified as subtasks. Reproduced focused memory-engine and normalizer probes; full findings task-6-review.md.
- Task6 minor (deferred): new suggestion-level diagnostics MigrationActivities.ts:552 and ScanV2.ts:66,88; report must distinguish from inherited lint/runtime noise. Final reviewer must triage.
- Task6 cannot-verify resolved: Task7 owns atomic public command CAS/deferred SQL proof; Task8–10 import/publication/finalization; Task11 production SQL history/process restart. Task6 real memory Activity and SQL projection evidence is correctly bounded, no missing Task6 gate hidden here.
- Task6 fix round1/5 dispatched original /root/task_06_implementer gpt-6-astra/high, base8326051c; covering MigrationWorkflow.test.ts, MigrationProjection.test.ts and Scan.test.ts/Manifest.test.ts. Scoped re-review required after commit/evidence.

- Ruling: Task6 fix1 replaces private boolean resume result with Schema-backed Resumed | AwaitRetry{failureSequence} | Rejected; preserve exact-generation CAS, join newer same-fence pending or already-accepted generations through their durable Retry token, reject stale fence/cleanup/cancellation/terminal separately. Each resume Activity includes operation key plus generation and the orchestration uses an iterative join loop — newer sibling failures are not superseded executions — cost if wrong: internal callback/schema fixture churn and retry liveness; barrier regressions for both newer pending and newer accepted generation plus SQL predicates required. No public HTTP contract or execution system change.

- Ruling: Task14 Schema.Unknown/rawPages search is a boundary audit, not a blanket zero-match rule: inspect every match and forbid bulk/unknown Workflow payloads or Activity success/error results, while allowing verified artifact reads inside Activities followed by named domain decoding — Task6 legitimately reads stored source arrays internally and its actual persisted results are reference-only — cost if wrong: a history leak could be overlooked; final reviewer must inspect declared schemas and encoded real-result tests, never satisfy grep by renaming aliases.

- Task6 fix1 commit875ae6af;37focused+8SQL tests pass after meaningfulRED, typecheck/lint/format exit0. Independent reviewer /root/task_06_reviewer gpt-6-astra/high resumed scoped package8326051c..875ae6af; two Important findings, verdictpending. Minor diagnostics remain explicitly deferred/report corrected.

- Task6 fix round1/5 (2addressed,0open — overlapping generations join safely; true subtasks classified separately; commits8326051c..875ae6af), independent scoped review clean.
- Task 6: complete (commits2c78bfdf..875ae6af, review clean except deferred Minor diagnostics).
- Task7 dispatch prepared; base875ae6afc73d8c9b1b30ebf7ba234ed509732a08; fresh implementer gpt-6-astra/high required for transaction/gate/cancellation protocol complexity.

- Task7 active implementer /root/task_07_implementer gpt-6-astra/high; base875ae6af; fresh brief/context/rulings/source-backed SQL transaction preflight provided, no parallel implementations.

- Read-only cleanup/write boundary helper /root/cleanup_write_boundary_preflight gpt-6-astra/high dispatched during sequential Task7 implementation; no code/tests/subagents. Concrete P17/Task9/11 risk: in-flight external write landing after cleanup. Research public Workflow interruption/quiescence and local storage semantics; controller ruling pending, output cleanup-write-boundary-preflight.md.

- Ruling: Task7 required cleanup interface may expose start plus awaitReset returning verified post-cleanup fence/revision; carry command identity needed for Task11 to await the same Effect Workflow completion, not a custom execution poller. Rescan CAS uses returned current revision and still rejects intervening commands — cleanup claim/release advances revision, so pre-cleanup captured revision is stale — cost if wrong: internal cleanup/result interface churn or reset liveness; Task7 fake must model claim/release revisions and verified removal, Task11 supplies real durable completion.

- Ruling: Task7 completeScan installs initial StartImport regardless of prior scan failureSequence; recordScanFailure installs Retry(scan), generic recordFailure installs Retry(import) only after accepted configuration exists and otherwise preserves initial gate. Late skip validation may use required materialization dependency only bound to the owned execution/attempt/revision checked by configure CAS — prevents consumed-token reuse and cross-attempt failed-ID authorization — cost if wrong: internal gate/checkpoint contracts require coordinated tests and Task9 composition; no public schema change. Corrected configuration gets new accepted revision, operational retry retains snapshot.

- Ruling: Task7 preserves discard no-expectedRevision input and NotFound|Validation errors; read owned row and send revision/fence to cleanup CAS, map Conflict to discard_not_allowed and infrastructure failure to distinct stable cleanup_failed Validation reason with safe structured logging — preserves public seam and race arbitration without disguising infrastructure failure as action ineligibility — cost if wrong: reason-to-message mapping adjustment; test submission failure retains recovery handle and retry/discard one-winner behavior.

- USER-REQUESTED PAUSE 2026-09-22: stop implementation for cross-machine continuation. Tasks1–6 complete and reviewed; last reviewed code875ae6af. Task7 partial/uncommitted/unreviewed, no final report or task commit. Active prior agents unavailable after user interruption; no matching test/worktree command processes found. Preserve four WIP files and ledger in portable handoff; document status in plan. No push/merge/publish.
- Task7 last checkpoint: saved commands/projection log11passed including actual shared-PgClient rollback/no-premature-release/gate-reuse test. Still pending forced signal failure, late skips, cancellation/reset integration, materialize callback proposal, final checks/report/commit/review. Earlier projection-green filename contains a failed run; latest commands-green supersedes it. Do not infer success from filenames.
- Cleanup/write preflight helper interrupted before report. Observations saved in cleanup-write-boundary-notes.md, provisional proposals NOT ratified. Task9/11 must resolve live/remote write quiescence; do not silently implement tombstones based on investigation messages.

- Pause documentation committed locally as ef3b2e06; code remains last reviewed875ae6af plus preserved Task7 WIP. Portable bundle includes docs commit, WIP patch, complete plan workspace and skill snapshot. No push.

- Linux restore 2026-09-22: verified archive 280 files and bundle; cloned at /home/wouter/web/project-project-t172-effect, HEAD ef3b2e06; four WIP SHA-256 match. Canonical ledger restored (bundle already tracked task-2-report.md, identical to transfer). Local .git/info/exclude ignores ledger. bun install succeeded; focused non-SQL baseline30pass/21skip; backend typecheck0. Docker access pending user assistance; no preview resource access. Main checkout untouched. See restore-context.md for current user instruction overrides.
- Task7 replacement implementer dispatched: /root/task07_implementer, gpt-6-astra/high, base ef3b2e06 plus verified WIP. Chosen for transaction/gate/cancellation concurrency. No parallel implementers or helpers. Review must cover total Task7 changes from875ae6af, including restored WIP.

- Ruling: Task7 adds required materialize callback to MigrationActivities and fourth required makeProjectionMigrationActivities argument, invoked after StartImport and before workflow success, using a named Readonly input alias with existing payload/executionId. Explicit scoped tests supply continuation now; Task9 supplies real materialization before Task11 production cutover — the current skeleton ends immediately at StartImport and cannot represent import retry/cancellation; this fills the approved injectable lifecycle boundary — cost if wrong: internal factory/fixture churn; no public API/dependency or production no-op.

- Ruling: Task7 cleanup claim/release takes an explicit required reset_import/discard mode with mode-specific eligible states; update isolated callsites rather than introducing a default. Reset can claim rescan-eligible hidden imports including corrected ready/needs_configuration and reconnect_required, with unchanged revision/fence/owner safeguards. Discard and delete-last remain failed/cancelled only; Task11 owns expire/post_success extension — existing late correction makes reset publicly eligible but failed/cancelled-only cleanup cannot perform it — cost if wrong: internal fixture/interface churn or invalid reset eligibility; covering real projection and command race tests required.

- Read-only local-main inventory: fetched current main from /home/wouter/web/project-project into restored clone refs/remotes/main-checkout/main (73fdd94), without changing either working branch. Six main-only commits, including899e066 native atom/data-layer migration and2f51d75 project route layout refactor. No merge/cherry-pick performed. Before Task12 reconcile current user frontend instructions with missing Api.ts foundation on this older feature branch; user no-merge boundary still applies.

- Linux Task7 checkpoint: replacement implementer /root/task07_implementer stopped BLOCKED on mandatory disposable PostgreSQL access. No commit; HEAD remains ef3b2e06, Task7 WIP extended. Materialize callback RED then17/17GREEN; final focused31pass/28skip; full backend843pass/96skip (before final SQL-only fixture adjustment), shared14pass, final backend typecheck0, formatting/diff checks clean. Exact pending work/SQL command and evidence limits in task-7-report.md. Cleanup-mode ruling approved but implementation awaits SQL RED. Independent review not yet performed; Task7 not complete. User Docker/database request remains pending.

- Linux resume after user started Docker: socket still denied and user not in docker group; Omarchy pkexec authentication successfully launched isolated projectproject-t172-local-postgres postgres16-alpine at127.0.0.1:55432 with database projectproject_effect_v4_t172. Verified direct pg connection/current_database. Same Task7 implementer resumed with explicit test URL; no preview access or main changes. Installed SDD6.4.1 skill matches archived snapshot SHA-256.

- Local storage prepared through pkexec: DockerHub minio/minio unavailable; used documented official quay.io/minio/minio image with digest in local-test-resources.md. Browser DB created separately. Source: https://github.com/minio/minio/blob/master/docs/docker/README.md . No project Docker Compose or runtime changes. Local conditional-PUT probe demonstrates IfMatch and IfNoneMatch reject after retirement with412; first two probes hit socket reuse reset, final per-operation client matches existing S3Storage lifecycle and passes. This is API capability evidence only, NOT approval or proof of any complete cleanup protocol; script/log in ledger workspace.

- Ruling: Task7 SQL command tests compose the public Sharding/Runners/RunnerStorage/RunnerHealth/ShardingConfig layers used by SingleRunner, substituting SqlMessageStorage.layerWith({prefix}) with a unique prefix per independent test, stable throughout all runtimes/replays in that test, sharing the same PgClient — SingleRunner.layer hardcodes default message tables, so persisted prior-test messages replay into new fixture closures and missing prior owners; no custom prefix option exists on installed helper — cost if wrong: test composition could diverge from production; keep source-equivalent topology and explicit SQL evidence. No private cluster SQL/table cleanup or production runtime change. Task11 restart tests must retain prefix across killed/restarted processes.

- User ruling, 2026-09-23: retain incomplete cleanup and recovery handles when remote upload outcome is uncertain. Do not add per-object retirement markers. Cleanup may remove its recovery handle only after every in-flight remote upload has a known terminal outcome and the graph is verified absent. The local MinIO conditional-request probe remains capability evidence, not proof of a complete protocol.

- User ruling, 2026-09-23: defer the Effect rc112 retry wakeup fix. Task7's blocked engine-dependent behavior remains a release blocker; do not patch or upgrade Effect as part of this Jira branch without a new decision.

- User ruling, 2026-09-23: pull latest GitHub origin/main before merging main into this feature checkout. Main fast-forward pull found `73fdd94b0c09e886116d727635b97f15973caa3b`; it has been merged into this checkout with Jira/backend conflict resolution and migration of the Jira frontend atoms to main's `Api.query`/optimistic-wrapper pattern. Shared, backend, and frontend typechecks pass. Frontend suite: 110 files, 648 tests pass. Backend suite: 71 files, 956 tests pass, 16 files/110 tests skipped without database env. New main lint rule currently flags five `make*Activity` imports in `MigrationWorkflow.ts`; those are pending Task9/11 workflow construction changes.

- Task7 Linux final-check checkpoint: focused real-SQL Jira68/68 and shared15/15 pass; backend typecheck exit0. Full backend exposed one intermittent new retry-wins arbitration-fixture timeout (935other passes,12skips). Same implementer investigating actual gate/teardown wait; no review or completion claim yet. User asks remaining duration and plans bed within an hour; estimate6–10hours for full remaining plan, Task7 review target within hour subject to checks.

- User workflow override 2026-09-22: skip independent reviews between tasks and run one comprehensive final review checking each task plus integration. Continue focused behavioral tests, typechecks, self-review and task commits; avoid redundant broad reruns on unchanged code. Tasks1–6 retain completed review evidence. Tasks7–14 may be marked implementation complete after covering verification, with independent review explicitly deferred; final review must cover every task, all unreviewed ranges and earlier deferred findings. Same Task7 implementer resumed, no Task7 reviewer will be dispatched. This explicit user instruction supersedes the SDD per-task review loop.

- Task7 real early-Retry regression confirms SQL engine lost wakeup: public Workflow.poll remains Suspended with accepted scanning projection; explicit test-only Workflow.resume observes stored Retry and finishes real runScanUnit. Same implementer preparing a clearly incomplete application checkpoint plus regression/report. No app resume/poll workaround or dependency change implemented. Controller asked user about a focused tracked Effect patch (architecture-rule interpretation, not explicit dependency-fix prohibition). Task8 pure builders can proceed independently after Task7 writes stop; Task7 remains incomplete and this release blocker goes to final review.

- Task7 checkpoint commitfaa228a: application commands and failing real early-Retry regression preserved; BLOCKED on Effect SQL lost wakeup, no dependency changes. Report and task-8-api-handoff.md complete. No independent per-task review by explicit user override.
- Task8 dispatched /root/task08_implementer, gpt-6-astra/high for pure schema/privacy/determinism integration; basefaa228a. Reads brief, publication rulings and accepted configuration API handoff. Sole implementer; Task7 engine blocker remains tracked while independent pure work advances. Final-review-checklist.md explicitly covers all14 tasks.

- User parallel-work override: execute concurrently wherever files are unique and interfaces stable, superseding SDD single-implementer restriction. Root retains file-ownership map and serializes shared git-index commits; final-only independent review stays in force.
- Parallel wave from checkpointfaa228a: /root/task08_implementer owns Task8 pure domain files; /root/task10_index_builders (gpt-5.6-sol/high) owns only Layers/TicketIndex.ts and TicketIndex.test.ts for Task10 Step3; /root/task13_fixtures (gpt-5.6-sol/high) owns only Jira/BrowserFixtures.ts and BrowserFixtures.test.ts for Task13 fixture portions. Root owns plan top/status and ledger. Task9 and publication/cleanup/harness integration remain dependent; no task completion inferred from partial extraction.
- Ruling: Task8 exclusion archive reconstructs schema-decoded owned records and filtered embedded collections; unknown inseparable mixed envelopes retain only non-content metadata/checksum/reason, while allowed records remain through decoded category records. Disclosure can retain raw envelopes — raw issue JSON may contain excluded comments/worklogs/rendered content and passthrough would leak it — cost if wrong: raw visible provenance may be reduced for unsupported envelope fields; keep category accounting and marker regressions across all permanent outputs.

- Root read-only upstream check found official rc.117 includes workflow wake-up/reset improvements; isolated dependency-only upgrade diagnostic could not load tests because Config.redacted was removed, and typecheck fails. Shared deps unchanged. See effect-upgrade-investigation.md and logs; no claim regression passes and no patch/upgrade chosen.

- Parallel checkpoints: Task8 initial publication-plan fixtures12pass; still implementing complete contracts/coverage. Task10 index builder tests RED2expected failures with9existing pass; exports/rebuild wiring in progress. Task13 fixture existing transport/OAuth seams confirmed, source-specific tests/implementation in progress. None of these are full task completion claims.
- Ruling: retain Task8 new contracts/builders in planned PublicationPlan.ts while legacy exports remain staged, rather than add a temporary internal module/re-export boundary — Task11 owns legacy removal and unnecessary file churn slows integration — cost if wrong: large interim file; report growth for final review after cutover. Resolved ref+Schema.Json inputs still require named domain decoding before traversal and exact manifest-reference validation.

- Task14 independent cleanup slice completed by controller as scoped git maintenance: fd6e16f removes exactly six planned unused banner-prototype JPGs. Pre-delete rg found references only in the plan deletion list, none in application code. No behavioral test needed for unused binaries; full Task14 browser/release verification remains pending. Other agents' unstaged work untouched, shared commit slot released.

- User status interruption: resumed all three existing agents without redispatching completed stages. Task10 index report11/11 complete, commit slot reserved to finish. Task8 latest requested pure-domain suite93/93 across7files; own typecheck clean, foreign Task13 fixture diagnostics remain. Task13 two fixture files now in progress. User informed no end-to-end version yet and several hours remain. No pending architecture choice was answered by the status question.

- USER STOP FOR TODAY:2026-09-22. All agents inactive; no task-related host process remains. Disposable PostgreSQL/MinIO containers stopped successfully, data preserved. Saved exact Task8/Task13 WIP and detailed next-day handoff; no further implementation authorized until user resumes. Task10 index slice35b8a70 complete11/11; Task8 last agent-reported93/93 with final closeout incomplete; Task13 fixture tests/closeout unknown. Pending cleanup/engine/frontend-merge questions remain unanswered.

- Final overnight WIP snapshot f6c2c64 committed all remaining Task8/Task13 work and tracked handoff. Ports55432/59000/59001 verified closed after successful container stop. No implementation resumed after stop. This is recovery state, not a passing full-suite or completion claim.
