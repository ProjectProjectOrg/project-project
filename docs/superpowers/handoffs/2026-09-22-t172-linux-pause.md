# T-172 resume tomorrow — paused 2026-09-22

User explicitly requested stopping for today. Do not continue implementation until they resume.

## Workspace and instructions

- Actual feature checkout: `/home/wouter/web/project-project-t172-effect`.
- Branch: `codex/T-172-effect-workflow`. Original `/home/wouter/web/project-project` stays untouched.
- Original tar already verified/extracted/applied; do NOT reapply it or restart Tasks1–6.
- Full plan: `docs/superpowers/plans/2026-09-22-t172-effect-jira-migration.md`.
- Design: `docs/superpowers/specs/2026-09-22-t172-effect-jira-migration-design.md`.
- Canonical detailed ledger/artifacts: `.superpowers/sdd/2026-09-22-t172-effect-jira-migration/`.
- User changed workflow: NO independent reviews between tasks; one comprehensive final review checking all14 tasks and their integration. Keep focused tests/typechecks and self-review; avoid redundant broad reruns.
- User explicitly requested parallel implementation whenever files are disjoint and interfaces stable. `parallel-work.md` records ownership. Serialize shared-index commits; no blanket git add/reset/stash of another agent's work.
- Current user type rules override old branch AGENTS: object type aliases wrapped in Readonly, nested wrappers, ReadonlyArray; no interfaces/per-field readonly; no explanatory code comments. Read docs/PROJECTPROJECT.md before nontrivial work and installed effect/AGENTS.md fully before Effect code. UI also needs PRODUCT/DESIGN/quality checklist and current atom skill.
- No push, merge, publication, shared branch writes or preview resources authorized. Origin points to the LOCAL handoff bundle, not GitHub. Local reference main-checkout/main is73fdd94; no merge performed.

## Saved state

Tasks1–6 are implemented and independently reviewed; last reviewed code875ae6afc73d8c9b1b30ebf7ba234ed509732a08. Never repeat them.

Recent commits before final pause snapshot:
- faa228a: Task7 application commands and early-Retry failing regression, explicitly WIP.
- 83113d4: parallel/final-only-review workflow in plan.
- fd6e16f: planned six unused banner-prototype JPGs removed; no application references. Only Task14 cleanup slice complete.
- 35b8a70: Task10 pure index-row builders; only TicketIndex.ts/.test.ts. Focused11/11 pass. Ordinary rebuild uses builders; full atomic publication remains pending. `task-10-index-report.md` has signatures/evidence.

Task8 unfinished code is preserved: Jira Adf, Mappings, Preflight, PublicationPlan, Report and tests. Agent `/root/task08_implementer` last reported93/93 requested pure-domain tests across7files at22:33:17, no Task8 typecheck errors; typecheck still saw Task13's concurrent fixture diagnostics. Final scoped lint/import-order formatting/report/commit were underway. NO final Task8 report was written before pause; do not infer complete verification from the checkpoint commit. The93/93 count is the implementer's reported evidence, not a root rerun; test output remained in the worker conversation rather than a saved log.

Task8 contracts described by agent: pure immutable preparation bound to manifestSha256 and accepted configurationRevision, actual settled attachment outcomes before final plan, canonical bytes plus external publicationRevision, deterministic project UUID/comment IDs/attachment ULIDs, exact final Markdown/index/reference rows and archive/report bytes. Task9 must write plan contents verbatim; attachment-reference rows derive from final ticket/comment Markdown. Named raw-envelope/category schemas must decode resolved ref+Schema.Json before traversal. Keep exclusion fail-closed: reconstruct allowed owned records/collections; inseparable unknown mixed envelope retains metadata/checksum/reason only. Raw disclosure mode can retain envelopes. Preserve all category accounting and restricted marker tests. `publication-protocol-rulings.md`, `task-8-context.md`, `task-8-api-handoff.md` remain binding. Keep planned PublicationPlan.ts scope during staged legacy compatibility; final review revisits final structure after Task11 removes legacy.

Task13 fixtures unfinished: `BrowserFixtures.ts` and `.test.ts` now exist, owned by `/root/task13_fixtures`. Existing transport/OAuth injection seams work; no production/runtime changes needed for this slice. Final tests/report were NOT received. Resume from these files, not initial reading. Scope was only fake Jira/OAuth/scenarios/categories/pagination/call counts/429/reconnect/paused attachment plus tests. Harness script, resource safety tests, control endpoints and public browser proof remain pending. Do not call Task13 complete.

Tasks9, Task10 publication integration, Task11 cleanup/production cutover/restart proof, Task12 UI, Task13 harness wiring and Task14 release/browser checks remain pending. Do not claim an end-to-end working migration or a release-ready branch.

## Confirmed Task7 blocker

`task-7-report.md` contains full evidence and commands. Real SQL early Retry is stored atomically with accepted projection, but can lose its wakeup and remain Workflow.Suspended. A diagnostic-only explicit Workflow.resume then reads stored Retry and finishes real runScanUnit. No application poll/resume workaround, dependency patch or upgrade implemented.

Installed Effect is4.0.0-rc.112. Focused application suite68/68 and shared15/15 passed before the added real early-Retry regression; full backend failed935pass/12skip/one retry timeout. Later regression confirms real issue, not just a flaky fixture. Final pre-pause Task7 typecheck/format clean. Do not increase timeout or await initial suspension to conceal the problem.

Read-only root investigation found official rc.117 includes newer wakeup handling; rc.113+ also has waitForRunReply/resumeGate. Shared deps unchanged. Isolated diagnostic clone `/tmp/projectproject-t172-effect117-yz6iyowb/repo` only upgraded four package manifests and installed dependencies. Regression suite cannot load because Config.redacted was removed; backend typecheck has API-migration/cascade failures. No passing upgrade claim. Source snapshots/provenance and diagnostic logs are in ledger; see `effect-upgrade-investigation.md`. Official release: https://github.com/Effect-TS/effect/releases/tag/effect@4.0.0-rc.117 . Temporary clone is diagnostic only, not the feature workspace.

## Pending user decisions — no answer/approval yet

1. Cleanup storage protocol: conditional uploads + empty retirement markers versus retaining incomplete cleanup/recovery handles when uploads are uncertain. Original handoff explicitly left this unapproved; no marker protocol implemented. Task9/11 must resolve before relying on complete cleanup with in-flight remote writes. See cleanup-linux-source-check.md and related notes. Local MinIO conditional probe proved API behavior only.
2. Focused tracked Effect dependency patch versus deferral. This question preceded discovery of newer official fixes; an upgrade would also require API migration and has not been approved as a substitute. Root clarified dependency-fix approval was its interpretation of architecture boundaries, not an explicit ban in AGENTS.
3. Merge current local main into feature branch to obtain required native frontend data layer, versus defer wizard work. Handoff explicitly reserves merges for approval. New user atom instructions require Api.query/request-object keys/wrapper refresh; old Task12 sketches contradict these. Merely copying Api.ts leaves two runtimes. See frontend-parallel-boundary.md. No merge performed.

Do not infer answers from user wanting faster/parallel work or going to bed.

## Environment stopped, data retained

Both task containers were stopped successfully with pkexec docker stop:
- projectproject-t172-local-postgres: loopback55432, postgres16-alpine; disposable databases projectproject_effect_v4_t172 and projectproject_effect_v4_t172_browser.
- projectproject-t172-local-minio: loopback59000 S3/59001 console; bucket projectproject-t172-local-browser.

Restart only when work resumes:
`pkexec /usr/bin/docker start projectproject-t172-local-postgres projectproject-t172-local-minio`

Docker socket is inaccessible to the unprivileged user; pkexec uses desktop authentication. No docker group or system configuration was changed. Never docker rm these containers if their local fixture data is wanted. Docker daemon is user-started and left alone.

Disposable SQL URL (development-only): postgres://projectproject:projectproject_dev@127.0.0.1:55432/projectproject_effect_v4_t172 . Export as PROJECTPROJECT_TEST_DATABASE_URL, unset unrelated DATABASE_URL. MinIO user/password projectproject/projectproject_dev. Browser database and bucket are separate from test DB. Never preserved preview resources.

All agents are inactive after the user's stop. No task-related test/dev process was found in /proc inspection (feature checkout and isolated upgrade clone); tool install/typecheck/test sessions already exited. Only the root checkpoint/shutdown commands ran afterwards.

## First actions tomorrow

1. Read this note, progress.md, git status/log and the persisted reports. Locate final pause snapshot commit; do not reapply archive patches.
2. Resume existing Task8/Task13 agents if available, otherwise give fresh agents their current files, briefs/context and checkpoint; do not reimplement completed work. Task10 builder commit is done.
3. Finish bounded Task8/fixture verification/reporting, accounting for their shared typecheck interaction; no per-task reviewer.
4. Resolve pending choices before dependent integration. Restart disposable containers as needed.
5. Continue maximum safe parallelism, retain final task-by-task review checklist and all earlier deferred findings. Final review range begins original base994455a26be958db47826c7425efedfdf84aea90, not merely the last checkpoint.
