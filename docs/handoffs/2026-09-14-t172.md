# T-172 Jira migration handoff

This is the portable resume point for branch `feat/T-172-migrate-a-jira-cloud-project-into-projectproject` as of 2026-09-14. The product design and feasibility sources remain:

- `docs/superpowers/specs/2026-09-14-jira-cloud-project-migration-design.md`
- `docs/spikes/2026-09-14-jira-cloud-migration-feasibility.md`
- `docs/jira-cloud-migration-research.md`

The scope corrections and implementation state below override stale portions of those documents.

## Delivered

- Jira ADF conversion and second-pass reference rewriting, including migration warnings and fixtures.
- Immutable Jira-origin comments with provenance, exact imported timestamps/bodies, canonical markdown persistence, and comment-index rebuild support.
- Jira OAuth connection/profile/disconnect, accessible sites/projects, encrypted rotating credentials, reconnect-required handling, and a typed paginating Jira Cloud/Agile client.
- Durable organization-scoped migration jobs with idempotent creation, revisions, lifecycle actions, leases, and the approved eight-endpoint API under `/api/orgs/:orgSlug/jira-migrations`.
- Pure manifest normalization, mapping requirements, preflight validation, and publication planning.
- A full-page wizard at `/orgs/:orgSlug/migrations/jira`, including source selection, resumable jobs, lifecycle screens, mapping forms, polling, configuration mutations, and terminal actions.
- A leased in-process scan worker that reads project metadata, issues, comments, worklogs, and Jira Agile data, then builds a versioned manifest for organization object storage.

Key implementation commits, oldest to newest: `2425afc3`, `3b576f20`, `e0c8540b`, `e787b586`, `83cd2912`, `21c0e97e`, `a016c03c`, `1ea2e85f`, `09268266`, `6268d4c4`, `97c7bf1c`, `231f1cf0`, `af2f3cc2`, `7c6d0ea1`.

## Current verified boundary

Focused Jira client and scan tests pass 9/9 on a disposable database. The focused OAuth/client/schema/database suite passed 39 tests, the pure migration suite passed 14 tests, and prior converter/comment packets were independently verified. Shared TypeScript passes; backend TypeScript reports zero errors but its checker still exits nonzero for configured Effect warnings/suggestions.

A user-authorized real scan was exercised without logging or committing source data. It reached scan progress 608/609 after reading project metadata, 301 issues, comments, and worklogs, then failed with a safe non-retryable `JiraError` in the remaining Agile tail: boards, sprints, or sprint memberships. Commit `7c6d0ea1` adds safe stage boundaries to identify the exact failing request on the next rescan. No completed manifest or report was published, mapping was not reached, and no duplicate migration was created.

The existing job can be resumed only if its database, encryption key, Jira connection, and organization storage are restored together. Otherwise reconnect Jira, reconnect storage, and start a new scan on the fresh machine.

## Approved contracts and scope

- Jira connections persist until explicit local disconnect. Token refresh is serialized and rotated tokens are persisted atomically. There is no application-callable Atlassian revoke operation.
- Jobs are durable, revisioned, leased, and resumable. Staged manifests use `migrations/jira/<migration-id>/`; successful archives use `imports/jira/<migration-id>/`. Failed/cancelled staging is retained for 30 days.
- Mapping requires explicit decisions for identities, existing destination statuses, issue types, priorities, tag collisions, restricted content, attachment skips, and competing active/future sprint memberships. Completed sprint memberships are preserved automatically.
- Publication builds an unreachable destination first, then exposes the project/index state and marks success in one PostgreSQL transaction. Recovery must never expose a partial project.
- Jira epics, versions, and sprints import into the existing Group domain and are reported by the wizard. New epic/milestone tabs, routes, chips, or management UI are deferred to separate tickets and are not a T-172 release gate.

## Remaining work

1. Rescan the same restored job on `7c6d0ea1`, use the new stage boundary to correct the final Jira Agile contract, and verify a durable manifest plus `needs_configuration` transition.
2. Read the stored manifest during execution; stream accepted attachments; create hidden canonical project, ticket, comment, tag, status, and Group data; rewrite references; and archive provenance.
3. Execute atomic publication, index rebuild/validation, success transition, retry/recovery, cancellation cleanup, explicit discard, and 30-day retention cleanup.
4. Close wizard gaps: save-and-leave persistence for dirty partial configuration, URL-backed/clamped step state, revision-conflict recovery copy, and optional attachment selection.
5. Run one final integration/security review and focused cross-stack verification, including browser coverage. Do not claim a completed migration before publication succeeds.

## Fresh-machine setup

Use the repository's normal install and database migration flow. Configure a local `.env` without committing it. Required variable names for this path are `DATABASE_URL`, `BETTER_AUTH_SECRET`, `BETTER_AUTH_URL`, `GITHUB_CLIENT_ID`, `GITHUB_CLIENT_SECRET`, `USER_SECRET_ENCRYPTION_KEY`, `JIRA_CLIENT_ID`, `JIRA_CLIENT_SECRET`, and `PROJECTS_DIR`. Jira's OAuth callback must be exactly:

```text
<BETTER_AUTH_URL>/api/integrations/jira/oauth/callback
```

Organization storage is configured in the application, not through S3 environment variables. For local MinIO use its reachable endpoint, region, pre-created bucket, access-key id, secret access key, optional key prefix, and `forcePathStyle: true`. The standard local endpoint is `http://localhost:9000`; use the machine-appropriate reachable host when the app runs in a container.

The prior preview PostgreSQL dump, `.env`, OAuth/session state, and MinIO objects are intentionally not in Git. To preserve the exact same job across machines, transfer the private database dump and any required MinIO volume/object backup through a secure channel, keep file permissions private, use the identical `USER_SECRET_ENCRYPTION_KEY`, and restore only into an empty database. Never send secrets or the dump through Git. Without that secure state transfer, use a clean database and reconnect/rescan.

## Suggested skills

- `superpowers:test-driven-development` for the final Agile-contract correction and publication behavior.
- `superpowers:systematic-debugging` when reproducing the staged Agile failure.
- `superpowers:verification-before-completion` before claiming manifest or publication success.
- `code-review` for the single final integration/security review after the pipeline is complete.
